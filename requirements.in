# --- PYTHON 3.8 PINS ---
numpy<1.25.0
pandas<2.1.0

# --- CORE LIBRARIES ---
sqlalchemy
oracledb
python-dotenv
pytest

# --- PREVENT DOWNGRADE ---
# We force these to be modern so they share the same 'typing-extensions'
pydantic>=2.0
pydantic-settings>=2.0